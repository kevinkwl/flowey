language: python
python:
  - '3.6'
install:
  - pip install -r flowey-server/requirements.txt

script:
  - ./test.sh

deploy:
  provider: reports
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master

